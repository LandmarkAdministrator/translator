# Core dependencies
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
pyyaml>=6.0.1
loguru>=0.7.2
tqdm>=4.66.1

# Audio processing
sounddevice>=0.4.6
soundfile>=0.12.1

# ASR (Speech-to-Text)
# Note: openai-whisper is NOT needed. faster-whisper replaces it entirely
# and is faster, smaller, and runs on CPU without CUDA.
faster-whisper>=1.1.0

# Translation
transformers>=4.35.0
ctranslate2>=4.0.0
sentencepiece>=0.1.99
sacremoses>=0.1.1
protobuf>=3.20.0

# TTS (Text-to-Speech)
piper-tts>=1.2.0
onnxruntime>=1.16.0

# PyTorch is installed separately by install.sh with the correct GPU backend.
# Do NOT install torch from this file — the GPU-specific index URL is required.
# See install.sh or docs/SETUP.md for the correct pip install command.

# IPC and utilities
psutil>=5.9.0
python-dotenv>=1.0.0

# Development / testing (not required for production)
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
